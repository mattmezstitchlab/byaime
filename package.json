{
  "name": "byaime-studio",
  "version": "1.0.0",
  "private": true,
  "description": "BYAIME — Studio créatif et technologique d'expériences numériques sur mesure.",
  "scripts": {
    "dev": "concurrently \"npm run watch\" \"npm run serve\"",
    "build": "npx @tailwindcss/cli -i css/input.css -o css/styles.css --minify && npx terser js/main.js -o js/main.min.js -c -m",
    "watch": "npx @tailwindcss/cli -i css/input.css -o css/styles.css --watch",
    "serve": "node server.js",
    "start": "node server.js"
  },
  "devDependencies": {
    "@tailwindcss/cli": "4.1.4",
    "browser-sync": "^3.0.3",
    "concurrently": "^9.1.2",
    "terser": "^5.46.1"
  }
}
